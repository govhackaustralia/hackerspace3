= form_for [:admin, @competition, @sponsor] do |f|
  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :website
    = f.url_field :website
  .field
    = f.label :description
    = f.text_field :description
  .actions
    = f.submit
