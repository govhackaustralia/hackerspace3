<main class="team_page">
  <h2>Edit Sponsor <%= @sponsor.name %></h2>
  <%= render 'form' %>
  <p><%= link_to 'Back to Sponsor', admin_competition_sponsor_path(@sponsor.competition_id, @sponsor) %></p>
</main>
