<main class="challenge_admin">
<h2>Create a New Challenge for <%= @region.name %> region</h2>

<%= render 'form' %>

<%= link_to "Back to #{@region.name} Region", admin_region_path(@region) %>
</main>
