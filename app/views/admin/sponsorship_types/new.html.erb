<h2>Create New Sponsorship Type</h2>

<%= form_for [:admin, @sponsorship_type] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name  %>
  <%= f.label :order %>
  <%= f.number_field :order, min: 1   %>
  <%= f.submit %>
<% end %>
