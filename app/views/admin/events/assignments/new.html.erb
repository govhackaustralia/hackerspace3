<%= render 'admin/assignments/user_search', locals: { path: new_admin_event_assignment_path } %>

<%= link_to "Back to #{@assignable.name} #{@assignable.class.to_s} Assignments", admin_event_assignments_path(@assignable) %>
