<% content_for :title, "New Flight for Event #{@event.name}" %>
<main class="team_page">
  <h2>
    <%= yield :title %>
  </h2>
  <%= render 'form' %>
  <%= link_to 'Back to Event Flights', admin_event_flights_path(@event) %>
</main>
