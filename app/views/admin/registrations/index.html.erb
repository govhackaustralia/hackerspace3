<main class="registrations_list">
	<section>

<h2>Registrations for <%= @event.name %> Event</h2>

<table>
	<thead>
		<tr>
			<th>Full Name</th>
			<th>Preferred Name</th>
			<th>Organisation</th>
			<th>Dietary</th>
			<th>Status</th>
			<th>Registration Type</th>
			<th>Parent/Guardian</th>
			<th>No Photos</th>
			<th>Data Cruncher</th>
			<th>Coder</th>
			<th>Creativer</th>
			<th>Facilitator</th>
			<th>Edit</th>
		</tr>
	</thead>
	<tbody>
<% @event.registrations.each do |registration| %>
	<tr>
		<td><%= registration.assignment.user.full_name %></td>
		<td><%= registration.assignment.user.preferred_name %></td>
		<td><%= registration.assignment.user.organisation_name %></td>
		<td><%= registration.assignment.user.dietary_requirements %></td>
		<td><%= registration.status %></td>
		<td><%= registration.assignment.user.registration_type %></td>
		<td><%= registration.assignment.user.parent_guardian %></td>
		<td><%= registration.assignment.user.request_not_photographed %></td>
		<td><%= registration.assignment.user.data_cruncher %></td>
		<td><%= registration.assignment.user.coder %></td>
		<td><%= registration.assignment.user.creative %></td>
		<td><%= registration.assignment.user.facilitator %></td>
		<td><%= link_to 'Edit', edit_admin_event_registration_path(@event, registration) %></td>
	</tr>
<% end %>
	</tbody>
</table>

<%= link_to 'New Registration', new_admin_event_registration_path(@event) %><br>
<%= link_to "Back to #{@event.name} Event", admin_region_event_path(@event.region, @event) %><br>

