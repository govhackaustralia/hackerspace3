<% if params[:mail_orders] %>
  <h2>Edit Mail Orders for Bulk Mail <%= @bulk_mail.name %></h2>
  <%= render 'mail_order_form' %>
<% else %>
  <h2>Edit Bulk Mail <%= @bulk_mail.name %></h2>
  <%= render 'form' %>
<% end %>

<%= link_to 'Back to Bulk Mail', admin_region_bulk_mail_path(@region, @bulk_mail) %>
