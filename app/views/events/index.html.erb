<% if params[:event_type] == COMPETITION_EVENT %>
  <%= render 'competition_events' %>
<% elsif params[:event_type] == AWARD_EVENT %>
  <%= render 'awards' %>
<% else %>
  <%= render 'all_events' %>
<% end %>
<%= link_to 'Download CSV', events_path(format: 'csv') %></br>
