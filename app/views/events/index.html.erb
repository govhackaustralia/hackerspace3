<% if params[:event_type] == CONNECTION_EVENT %>
  <%= render 'connections' %>
<% elsif params[:event_type] == COMPETITION_EVENT %>
  <%= render 'competition_events' %>
<% else %>
  <%= render 'all_events' %>
<% end %>
<%= link_to 'Download CSV', events_path(format: 'csv') %></br>
