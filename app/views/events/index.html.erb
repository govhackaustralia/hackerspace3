<% if params[:event_type] == STATE_CONNECTION %>
  <%= render 'connections' %>
<% elsif params[:event_type] == COMPETITION_EVENT %>
  <%= render 'competition_events' %>
<% else %>
  <%= render 'all_events' %>
<% end %>
