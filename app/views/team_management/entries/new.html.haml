- content_for :title, 'New Entry'
%main.team_page
  %section
    %h2= "Enter #{@challenge.name} Challenge for Team #{@team.name}"
    = render 'form'
    = link_to 'Back to Challenge', challenge_path(@challenge.identifier)
